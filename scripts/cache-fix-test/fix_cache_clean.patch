diff --git a/src/client.cpp b/src/client.cpp
--- a/src/client.cpp
+++ b/src/client.cpp
@@ -1370,6 +1369,17 @@ void ContextImpl::cacheClean(const std::string& name, Context::cacheAction actio
             }
         }
     }
+
+    // compact chanByCID: remove expired entries from swept channels
+    {
+        auto next(chanByCID.begin()),
+             end(chanByCID.end());
+        while(next!=end) {
+            auto cur(next++);
+            if(cur->second.expired())
+                chanByCID.erase(cur);
+        }
+    }
 }
 
 void ContextImpl::cacheCleanS(evutil_socket_t fd, short evt, void *raw)
